- added the discord invite link to be shown when doing /discord
- added linking (beta)
- fixed it to only show one advancement message in the game chat
- added the ability for webhooks
- added styled text support
  - did this because a custom advancement i made on a server printed as "§oSPEEEEEED" instead of being italics in the discord
- moved everything into its own folder in /config/
- added some more config files
- fixed the status not working
- fixed console channel
- added /discord help
- added more ways to display the online status
- changed the format of a few things
- removed the need for the JDA Library mod dependency
- fixed some small things
- added a no permission message
 - configurable in config
- added some more placeholders
- fixed it to show as gigabytes (GB) if the RAM is over 1024 megabytes (MB)
- added first time join message