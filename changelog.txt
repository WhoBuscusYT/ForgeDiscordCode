- added streaming to the bot's status
- added mod prefix to config
  - when displaying a public/private message in chat, it will display that first then the message, example: "[ForgeDiscord] Mod Reloaded"
- added the /Discord Config command
  - am not going to explain the sub-commands within it as that when i did that in the last update, it made the changelog hella big
 v0.0.2
- Added console channel
  - will output everything that is displayed in console
  - when you send a message inside of the channel, it will execute the message as a command via console
- made the high server usage ping only ping for RAM and not CPU
- added the ability to use webhooks for player chats
- fixed the discord -> minecraft chat not working
- added it to show the death reason for dying and not just "Player died"
- changed the way the high server usage message displays in discord